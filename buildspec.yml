{
    "version": "0.2",
    "phases": {
        "pre_build": {
            "commands": [
                "cat passedvariable.json | jq '.passedvar'",
                "export CODEBUILD_RESOLVED_SOURCE_VERSION=`cat passedvariable.json | jq '.passedvar'`",
                "echo $CODEBUILD_RESOLVED_SOURCE_VERSION"
            ]
        }
    }
}
