{
    "version": "0.2",
    "phases": {
        "pre_build": {
            "commands": [
                "ls -ltr",
                "source variable.sh",
                "echo $CODEBUILD_RESOLVED_SOURCE_VERSION"
            ]
        }
    }
}
